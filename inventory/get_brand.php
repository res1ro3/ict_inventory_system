<?php 
    require_once('../dbConfig.php');

    $brand_id = $_GET['brand_id'];
    echo "test";
    // $sql = "
    //     SELECT * FROM brands WHERE brand_id = :bi
    // ";
    // $query = $conn->prepare($sql);
    // $query->bindParam(':bi',$brand_id,PDO::PARAM_STR);
    // $row = $query->fetch(PDO::FETCH_ASSOC);
    // echo json_encode($row);
?>